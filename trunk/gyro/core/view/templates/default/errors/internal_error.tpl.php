<?php 
$title = tr('Server error', 'core');
$page_data->head->title = $title;
?>

<h1><?=$title?></h1>

<p class="error"><?=tr('Sorry, an internal error occured, so this page could not be build.', 'core')?></p>
